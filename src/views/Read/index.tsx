import ReactMarkdown from 'react-markdown';

import spec from 'assets/specsMarkdown.md';

import * as S from './styles';

export default function Read() {
	return <S.Wrapper>{/* <ReactMarkdown children={spec} /> */}</S.Wrapper>;
}
